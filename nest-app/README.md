# Nest App

Minimal Nest.js scaffold.

Run locally:

```bash
cd tst/nest-app
npm install
npm run build
npm start
```

Development (fast reload):

```bash
npm run start:dev
```

Endpoints:
- `GET /` → "Hello from Nest"
- `GET /ping` → `{ "pong": true }`
